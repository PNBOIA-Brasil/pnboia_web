<div class="container">
  
  <div class="d-flex justify-content-center pt-5">
    <%= image_tag "logo_pnboia.png", class: "nav-img" %>
  </div>

  <div class='container pt-5'>  
    <h3 class="text-center bg-primary text-white p-2 font-weight-bold">
    Esqueceu sua senha?</h3>
    
    <div class= 'container border border-primary'>
      <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Reinicie minha senha", class: "btn btn-success" %>
        </div>
      <% end %>
    </div>
    <%= link_to "Entrar", new_user_session_path, class: "btn btn-primary font-weight-bold" %>
    <%= link_to "Registrar-se", new_user_registration_path, :class => 'btn btn-primary font-weight-bold'  %>
    <%= link_to "Voltar", root_path, :class => 'btn btn-primary font-weight-bold'  %>
  </div> 
</div>
